spring:
  application:
    name: ts-route-service
  datasource:
    url: jdbc:mysql://ts-route-mysql:3306/ts-route-mysql?useSSL=false
#    url: jdbc:mysql://10.176.122.80:25777/ts-route-mysql?useSSL=false
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

# HTTP Server
server:
  port: 11178  # HTTP (Tomcat) port

swagger:
  controllerPackage: route.controller