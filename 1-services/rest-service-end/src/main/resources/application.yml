spring:
  application:
    name: rest-service-end
    
  rabbitmq:
    host: rabbitmq


      
  cloud:
    stream:
      bindings:
        output:
          destination: my-rest-service-queue-1
          contentType: text/plain
          binder: rabbit

server:
  port: 16000