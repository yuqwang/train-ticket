server:
  port: 18673

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:nacos-0.nacos-headless.default.svc.cluster.local,nacos-1.nacos-headless.default.svc.cluster.local,nacos-2.nacos-headless.default.svc.cluster.local}
  application:
    name: ts-inside-payment-service

  data:
    mongodb:
      host: ts-inside-payment-mongo
#     username: user
#     password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

swagger:
  controllerPackage: inside_payment.controller