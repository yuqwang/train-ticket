server:
  port: 18673

spring:
  application:
    name: ts-inside-payment-service

  datasource:
    url: jdbc:mysql://ts-inside-payment-mysql:3306/ts-inside-payment-mysql?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
swagger:
  controllerPackage: inside_payment.controller