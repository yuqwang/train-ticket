server:
  port: 18856

spring:
  application:
    name: ts-food-service
  data:
    mongodb:
      host: ts-food-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017
management:
  endpoints:
    web:
      exposure:
        include: prometheus
swagger:
  controllerPackage: foodsearch.controller