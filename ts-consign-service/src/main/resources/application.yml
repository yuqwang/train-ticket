spring:
  application:
    name: ts-consign-service
  data:
    mongodb:
      host: ts-consign-mongo
      database: ts
      port: 27017

management:
  endpoints:
    web:
      exposure:
        include: prometheus
# HTTP Server
server:
  port: 16111

swagger:
  controllerPackage: consign.controller