server:
  port: 12342

spring:
  application:
    name: ts-user-service
  datasource:
    url: jdbc:mysql://ts-user-mysql:3306/ts-user-mysql?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

swagger:
  controllerPackage: user.controller