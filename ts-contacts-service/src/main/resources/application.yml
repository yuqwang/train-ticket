server:
  port: 12347

spring:
  application:
    name: ts-contacts-service
  datasource:
    url: jdbc:mysql://ts-contacts-mysql:3306/ts-contacts-mysql?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

swagger:
  controllerPackage: contacts.controller