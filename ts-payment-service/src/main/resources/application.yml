# HTTP Server
server:
  port: 19001  # HTTP (Tomcat) port

spring:
  application:
    name: ts-payment-service
  datasource:
    url: jdbc:mysql://ts-payment-mysql:3306/ts-payment-mysql?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update

swagger:
  controllerPackage: com.trainticket.controller